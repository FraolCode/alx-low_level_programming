#include <stdio.h>

/**
 * main - prints all single digit numbers from 0 to 9
 * base 10
 * Return: return 0 if it is success
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; n++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
