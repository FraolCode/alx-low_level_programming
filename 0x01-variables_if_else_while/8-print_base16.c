#include <stdio.h>

/**
 * main - print all hexadecimal numbers
 * in lowercase
 * followed by new line
 * Return: return 0 if it is successful
 */
int main(void)
{
	int num;
	char alp;

	for (num = 48; num < 58; num++)
	{
		putchar(num);
	}
	for (alp = 'a'; ch <= 'f'; c++)
	{
		putchar(alp);
	}
	putchar('\n');
	return (0);
}
